spring:
  application:
    name: service-user

  data:
    mongodb:
      uri: mongodb://localhost:27017/test

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081



